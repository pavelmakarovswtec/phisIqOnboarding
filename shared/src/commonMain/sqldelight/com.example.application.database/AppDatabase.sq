CREATE TABLE User(
id INTEGER PRIMARY KEY AUTOINCREMENT,
userName TEXT,
avatarUri TEXT,
url TEXT,
userType TEXT
);

insertUser:
INSERT INTO User(userName, avatarUri, url, userType)
VALUES ?;

getAllUsersInfo:
SELECT User.*
FROM User;

getUserInfo:
SELECT User.*
FROM User
WHERE id = ?;
